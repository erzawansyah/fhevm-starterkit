# Simple Storage

This starter provides a minimal `SimpleStorage` Solidity contract and a basic test.

Usage

- Copy the contract into your Hardhat project contracts directory.
- Run tests: `npx hardhat test --grep SimpleStorage`
