# Simple Voting

This starter provides a minimal `SimpleVoting` contract and a basic test that demonstrates voting flow.

Usage

- Copy the contract into your Hardhat project contracts directory.
- Run tests: `npx hardhat test --grep SimpleVoting`
